name: mobile_app_ca
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.10.4

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter

  cupertino_icons: ^1.0.8
  google_fonts: ^6.0.0
  lottie: ^3.3.2

  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/